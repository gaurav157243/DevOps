# In AWS, create a new IAM user and assign the following policy - AmazonEC2ContainerRegistryFullAccess
# create access key for that user to allow other applications to perform actions 
# create a private ecr repositry (not public)
# in Github --> Settings -> Secrets and variables --> Actions --> Add two variables
##   AWS_ACCESS_KEY_ID and value is the IAM user accees key 
##   AWS_SECRET_ACCESS_KEY and value is IAM user secret access key
